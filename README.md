# sql-examples
List of small SQL snippets and scripts for example
  1. cntColName.sql - Adding columns to an existing table
  2. rebuild_table_with_column_reorder.py - rebuild a table with new columns while preserving the data
